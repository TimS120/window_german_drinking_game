{
  "rules": {
    "rooms": {
      "$roomCode": {
        ".read": "auth != null",
        ".write": "auth != null",
        "state": {
          ".validate": "newData.hasChildren(['players', 'currentPlayerIdx', 'deck', 'cardGrid', 'faceUp', 'version', 'updatedAt'])"
        }
      }
    }
  }
}
